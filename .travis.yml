sudo: required

language: go

services:
  - docker
branches:
  only:
    - master  

install:
  - docker build -t jonathan99/go-base:1.0 .

script: 
  - "sh ./script/docker-push/docker-push.sh"
  - docker run -it -p 8080:8000 --name api-go jonathan99/go-base:1.0
  - docker ps -a  
